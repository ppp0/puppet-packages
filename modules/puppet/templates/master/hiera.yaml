---
:backends:
  - json
:hierarchy:
  - fqdn/%{::domain_part_1}/%/%/%
  - fqdn/%{::fqdn_reversed}
  - clientcert/%{::clientcert}
  - environment/%{::environment}
  - defaults

:json:
  :datadir: <%= @hieraDataDir %>
